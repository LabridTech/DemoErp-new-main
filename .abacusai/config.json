{
  "permissions": {
    "allow": [
      "Bash(npm run lint 2>&1 || true)",
      "Bash(npm run lint 2>&1 || true)",
      "Bash(npm run type-check 2>&1 || true)",
      "Bash(npm run build 2>&1 || true)"
    ]
  }
}